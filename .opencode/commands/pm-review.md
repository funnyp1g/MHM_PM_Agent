---
description: 按统一 PM Agent 规范评审既有 PRD 或需求方案。
agent: pm
---

# PRD 评审

开始执行前，必须读取并严格遵循 `pm-agent-core/AGENT.md`。该文件是所有宿主 Agent 共用的唯一执行入口和权威规则源。

## 评审对象

$ARGUMENTS

按照统一规范加载产品知识，检查 PRD 的完整性、可落地性、业务规则、功能关联、页面与交互、证据口径、风险依赖和完成状态，运行校验脚本，并按“阻塞 / 重要 / 建议”输出问题。涉及页面但未制作原型时，明确记录未制作原型不影响本次 PRD 评审。
